{
  "name": "vibecheck-content-factory",
  "version": "1.0.0",
  "description": "Generates guided meditation scripts via GPT-4o for VibeCheck.",
  "type": "module",
  "main": "generate_sessions.js",
  "scripts": {
    "generate": "node generate_sessions.js"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "node-fetch": "^3.3.2"
  }
}
